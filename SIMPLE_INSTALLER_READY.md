# 📦 Простой установщик ГОТОВ!

## ✅ Что создано:

### 1. **simple-installer.bat** - Установщик
Устанавливает приложение в `%APPDATA%\WallNux\`

### 2. **uninstaller.bat** - Деинсталлятор
Полностью удаляет приложение

### 3. **WallNux.vbs** - Launcher (создается при установке)
Автоматически запускает сервер → браузер

## 🚀 Как использовать:

### Вариант 1: Простой (БЕЗ упаковки)

Просто дай пользователю:
```
simple-installer.bat
```

Пользователь:
1. Копирует всю папку проекта
2. Запускает `simple-installer.bat`
3. Готово!

### Вариант 2: С упаковкой (7-Zip SFX)

```bash
create-installer.bat
```

Создаст `WallNux-Messenger-Installer.exe` - один файл!

Требует 7-Zip: https://www.7-zip.org/

## 📁 Что делает установщик:

### При установке:
1. Создает папки в `%APPDATA%\WallNux\`
   - `Server\` - серверные файлы (скрыто от пользователя)
   - `Web\` - веб-файлы
2. Копирует все файлы
3. Создает `WallNux.vbs` launcher
4. Создает ярлыки (рабочий стол + меню Пуск)
5. Спрашивает про автозагрузку
6. Запускает приложение

### WallNux.vbs делает:
```vbs
1. Проверяет: запущен ли node.exe с server.js?
2. Если НЕТ: запускает node server.js в фоне
3. Ждет 3 секунды
4. Открывает http://localhost:3000 в браузере
```

### При удалении:
1. Останавливает все процессы
2. Удаляет из автозагрузки
3. Удаляет ярлыки
4. Удаляет все файлы
5. Очищает реестр

## 🎯 Для пользователя:

### Установка:
1. Запустить `simple-installer.bat`
2. Следовать инструкциям
3. Готово!

### Использование:
1. Двойной клик на ярлык "WallNux Messenger"
2. Автоматически запустится сервер
3. Откроется в браузере
4. Всё работает!

### Удаление:
1. Меню Пуск → WallNux → Удалить
2. Или запустить `%APPDATA%\WallNux\uninstaller.bat`

## 💡 Преимущества:

- ✅ Не нужен Electron (меньше размер)
- ✅ Работает в любом браузере
- ✅ Сервер запускается автоматически
- ✅ Простая установка
- ✅ Чистое удаление
- ✅ Автозагрузка (опционально)

## 📊 Размер:

- Без упаковки: ~100 MB (папка)
- С упаковкой (7z): ~30 MB (.exe)

Намного меньше чем Electron!

## ⚠️ Требования:

### Для пользователя:
- Windows 7 или новее
- Node.js (должен быть установлен!)
- Любой браузер

### Если Node.js не установлен:
Добавь в `simple-installer.bat` проверку и установку Node.js:

```batch
where node >nul 2>nul
if %errorlevel% neq 0 (
    echo Node.js не найден!
    echo Скачиваем Node.js...
    :: Скачать и установить Node.js
)
```

## 🚀 Быстрый старт:

### Для разработчика:
```bash
# Просто дай пользователю
simple-installer.bat
```

### Для распространения:
```bash
# Создай один .exe файл
create-installer.bat
```

## 🎉 Готово!

Теперь у тебя:
- ✅ Простой установщик (батник)
- ✅ Автоматический запуск сервера
- ✅ Ярлыки и автозагрузка
- ✅ Чистое удаление

Пользователь просто:
1. Запускает установщик
2. Кликает на ярлык
3. Всё работает! 🚀

## 📝 Примечание:

Если хочешь чтобы Node.js был встроен (не требовал установки):
1. Скачай Node.js Portable
2. Положи в папку `node-portable\`
3. Обнови пути в `WallNux.vbs`

Тогда вообще ничего не нужно устанавливать! 💪
