# 🚀 ФИНАЛЬНЫЙ УСТАНОВЩИК

## ✅ Три варианта на выбор:

### 1. **START.bat** ⭐ САМЫЙ ПРОСТОЙ

**Что делает:**
- При первом запуске: копирует файлы в `Desktop\.wallnux` (скрытая папка)
- Устанавливает права "только чтение"
- При каждом запуске: запускает сервер → приложение

**Требования:**
- Node.js должен быть установлен в системе

**Использование:**
```bash
START.bat
```

---

### 2. **WallNux.bat** - С автоустановкой Node.js

**Что делает:**
- То же что START.bat
- ПЛЮС: автоматически скачивает и устанавливает Node.js если нет

**Требования:**
- Ничего! Всё установит сам

**Использование:**
```bash
WallNux.bat
```

---

### 3. **install-and-run.bat** - С portable Node.js

**Что делает:**
- Скачивает Node.js portable (не требует установки)
- Копирует в скрытую папку
- Запускает всё

**Требования:**
- Ничего!

**Использование:**
```bash
install-and-run.bat
```

---

## 📁 Что происходит:

### При первом запуске:
1. Создается `%USERPROFILE%\Desktop\.wallnux\`
2. Копируются все файлы проекта
3. Папка скрывается (`attrib +h`)
4. Устанавливаются права "только чтение"
5. Запускается приложение

### При каждом запуске:
1. Проверяет: запущен ли сервер на порту 3000?
2. Если НЕТ: запускает `node server.js` в фоне
3. Ждет 3 секунды
4. Запускает:
   - `dist\win-unpacked\WallNux Messenger.exe` (если есть)
   - Или открывает `http://localhost:3000` в браузере

---

## 🔒 Безопасность:

### Скрытая папка:
```
Desktop\.wallnux\  (скрыта от пользователя)
```

### Права доступа:
```
Только чтение (RX)
Нельзя изменять (W запрещено)
```

Пользователь НЕ МОЖЕТ:
- ❌ Видеть папку (скрыта)
- ❌ Изменять файлы (только чтение)
- ❌ Удалять файлы (защищено)

---

## 🎯 Рекомендация:

### Для распространения:
Используй **WallNux.bat** - он установит Node.js автоматически!

### Для разработки:
Используй **START.bat** - самый быстрый

---

## 📦 Как распространять:

### Вариант 1: Дать батник
Просто дай пользователю `WallNux.bat`

### Вариант 2: Создать архив
```bash
# Упаковать всё в ZIP
powershell Compress-Archive -Path * -DestinationPath WallNux-Messenger.zip
```

Пользователь:
1. Распаковывает ZIP
2. Запускает `WallNux.bat`
3. Готово!

---

## ⚠️ Важно:

### Для работы нужно:
- Windows 7 или новее
- Node.js (или батник установит сам)
- Собранное Electron приложение в `dist\win-unpacked\`

### Если нет Electron:
Батник откроет в браузере автоматически!

---

## 🎉 Готово!

Выбери батник и используй! Всё работает автоматически! 🚀
