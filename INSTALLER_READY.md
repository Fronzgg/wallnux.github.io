# 📦 Установщик ГОТОВ!

## ✅ Что создано:

Полноценный Windows установщик (NSIS) который:

1. ✅ Устанавливает приложение в `%APPDATA%\WallNux`
2. ✅ Создает ярлыки на рабочем столе и в меню Пуск
3. ✅ Автоматически запускает сервер перед приложением
4. ✅ Предлагает добавить в автозагрузку
5. ✅ Создает деинсталлятор

## 🚀 Как собрать установщик:

```bash
build-installer.bat
```

Это займет 5-10 минут.

## 📦 Что получится:

В папке `dist/`:
- `WallNux Messenger-Setup-1.0.0.exe` (~150 MB)

Это полноценный установщик!

## 🎯 Как работает установщик:

### При установке:
1. Копирует все файлы в `%APPDATA%\WallNux\`
2. Создает скрипт `start-server.vbs`
3. Создает ярлыки которые запускают через VBS
4. Спрашивает про автозагрузку
5. Регистрирует в "Программы и компоненты"

### При запуске (через ярлык):
1. VBS скрипт проверяет запущен ли сервер
2. Если нет - запускает `node server.js` в фоне
3. Ждет 3 секунды
4. Запускает `WallNux Messenger.exe`

### При удалении:
1. Останавливает все процессы
2. Удаляет все файлы
3. Удаляет ярлыки
4. Удаляет из автозагрузки
5. Удаляет из реестра

## 💡 Преимущества:

- ✅ Пользователь не видит серверные файлы
- ✅ Сервер запускается автоматически
- ✅ Node.js встроен (не нужно качать)
- ✅ Работает из любой учетной записи
- ✅ Чистое удаление

## 🔧 Что внутри:

### Структура после установки:
```
%APPDATA%\WallNux\
  ├── WallNux Messenger.exe
  ├── start-server.vbs (launcher)
  ├── resources\
  │   └── app\
  │       ├── server.js
  │       ├── database.js
  │       ├── discord_clone.db
  │       └── node_modules\
  └── uninstall.exe
```

### start-server.vbs:
```vbs
1. Проверяет: запущен ли node.exe с server.js?
2. Если нет: запускает сервер в фоне
3. Ждет 3 секунды
4. Запускает WallNux Messenger.exe
```

## 📊 Размер:

- Установщик: ~150 MB
- После установки: ~200 MB
- Это нормально - внутри Node.js + Chromium!

## 🎉 Для пользователя:

### Установка:
1. Скачать `WallNux Messenger-Setup-1.0.0.exe`
2. Запустить установщик
3. Нажать "Далее" несколько раз
4. Готово!

### Использование:
1. Двойной клик на ярлык "WallNux Messenger"
2. Всё работает автоматически!

### Удаление:
1. Панель управления → Программы и компоненты
2. Найти "WallNux Messenger"
3. Нажать "Удалить"

## ⚠️ Требования:

### Для сборки:
- Windows
- Node.js
- npm

### Для пользователя:
- Windows 7 или новее
- Ничего больше!

## 🚀 Следующие шаги:

1. **Собери установщик:**
   ```bash
   build-installer.bat
   ```

2. **Протестируй:**
   - Запусти установщик
   - Установи приложение
   - Проверь что всё работает
   - Удали через "Программы и компоненты"

3. **Распространяй:**
   - Загрузи на сайт
   - Или на GitHub Releases
   - Или на Google Drive

## 💪 Готово!

Теперь у тебя полноценный установщик как у настоящих приложений!

Пользователи просто:
1. Скачивают .exe
2. Устанавливают
3. Запускают
4. Всё работает! 🎉

Собирай: `build-installer.bat`
