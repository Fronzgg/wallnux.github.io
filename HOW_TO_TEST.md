# 🧪 Как протестировать систему добавления в друзья

## Проблема:
Результаты поиска не отображаются на странице, хотя в консоли видно что данные получены.

## Решение 1: Тестовая страница

Я создал специальную тестовую страницу `test-search.html` для отладки.

### Шаги:

1. **Запустите сервер** (если еще не запущен):
```bash
node server.js
```

2. **Войдите в систему** на главной странице:
   - Откройте http://localhost:3000
   - Войдите в аккаунт

3. **Откройте тестовую страницу**:
   - http://localhost:3000/test-search.html

4. **Выполните поиск**:
   - Введите имя пользователя
   - Нажмите "Найти" или Enter

5. **Проверьте результаты**:
   - Результаты должны отобразиться на странице
   - В логе внизу будет подробная информация
   - Кнопки должны показывать правильный статус

### Что проверяет тестовая страница:

✅ Наличие токена авторизации
✅ Запрос к `/api/users/search`
✅ Получение данных с сервера
✅ Отображение результатов
✅ Статус дружбы (друзья/запрос отправлен/можно добавить)
✅ Отправку запроса в друзья

## Решение 2: Очистка кеша

Если тестовая страница работает, но основная нет - проблема в кеше браузера.

### Вариант A: Жесткая перезагрузка
1. Откройте http://localhost:3000
2. Нажмите **Ctrl+Shift+R** (Windows/Linux) или **Cmd+Shift+R** (Mac)

### Вариант B: Очистка кеша
1. Нажмите **F12** (открыть DevTools)
2. Нажмите **Ctrl+Shift+Delete**
3. Выберите "Кэшированные изображения и файлы"
4. Нажмите "Удалить данные"
5. Перезагрузите страницу

### Вариант C: Режим инкогнито
1. Откройте новое окно в режиме инкогнито
2. Перейдите на http://localhost:3000
3. Войдите в систему
4. Попробуйте поиск

## Решение 3: Проверка в консоли

Откройте консоль браузера (F12) и выполните:

```javascript
// Проверить что функция существует
console.log(typeof searchUsers);  // должно быть "function"

// Проверить что элемент существует
console.log(document.getElementById('searchResults'));  // должен быть элемент

// Вручную вызвать поиск
document.getElementById('searchUserInput').value = 'test';
searchUsers();
```

## Решение 4: Проверка сервера

В терминале где запущен сервер должны появляться логи:

```
🔍 Search users request: username from user: 1
📋 Found 1 users matching query
👤 User: username Status: none
✅ Sending 1 results with status
```

Если логов нет - сервер не получает запрос.

## Что делать если ничего не помогает:

1. **Перезапустите сервер**:
```bash
# Ctrl+C для остановки
node server.js
```

2. **Проверьте что сервер запущен на правильном порту**:
   - Должно быть: `Server running on port 3000`

3. **Проверьте что вы вошли в систему**:
   - localStorage.getItem('token') должен возвращать токен

4. **Используйте тестовую страницу** test-search.html для отладки

## Ожидаемый результат:

После поиска пользователя должны появиться карточки:

```
┌─────────────────────────────────────┐
│  [S]  spyderfronz7                  │
│       user@example.com              │
│                  [Добавить в друзья]│
└─────────────────────────────────────┘
```

С кнопками в зависимости от статуса:
- "Добавить в друзья" - зеленая, активная
- "Запрос отправлен" - серая, неактивная
- "✓ Уже в друзьях" - зеленая, неактивная
- "Ожидает ответа" - синяя, неактивная
